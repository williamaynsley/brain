# Brain — performance marketing

William's thinking space. `content.json` + `notes/` are the source; `bin/brain build` regenerates `index.html`.
Deployed via Vercel (auth-protected). Client documents do NOT belong in this repo.
